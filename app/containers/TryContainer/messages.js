/*
 * TryContainer Messages
 *
 * This contains all the text for the TryContainer container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.TryContainer";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the TryContainer container!"
  }
});
